on:
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install SFDX
        run: |
          sudo npm -g install sfdx-cli@stable
          sfdx --version
          sfdx plugins --core
      - name: Install SFDX Scanner
        run: |
          sudo sfdx plugins:install @salesforce/sfdx-scanner
          sfdx plugins
      - name: Run SFDX Scanner
        run: |
          sfdx scanner:run --target "force-app/main/default/classes/" --format json --violations-cause-error
